import { ComponentFixture, TestBed } from '@angular/core/testing';
import { PsymentaprpovalsPage } from './psymentaprpovals.page';

describe('PsymentaprpovalsPage', () => {
  let component: PsymentaprpovalsPage;
  let fixture: ComponentFixture<PsymentaprpovalsPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(PsymentaprpovalsPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
