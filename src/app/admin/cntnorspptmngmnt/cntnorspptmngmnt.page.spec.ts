import { ComponentFixture, TestBed } from '@angular/core/testing';
import { CntnorspptmngmntPage } from './cntnorspptmngmnt.page';

describe('CntnorspptmngmntPage', () => {
  let component: CntnorspptmngmntPage;
  let fixture: ComponentFixture<CntnorspptmngmntPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(CntnorspptmngmntPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
